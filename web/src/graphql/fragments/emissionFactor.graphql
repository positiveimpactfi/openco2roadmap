fragment EmissionFactorFragment on EmissionFactor {
  id
  name
  source
  dataSourceType
  geographicalArea
  values {
    id
    value
    startDate
    endDate
  }
  physicalQuantity {
    name
    baseUnit {
      name
      shorthand
    }
  }
  emissionSources {
    id
    name
  }
}
