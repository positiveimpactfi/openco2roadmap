fragment EmissionFactorFragment on EmissionFactor {
  id
  name
  source
  dataSourceType
  values {
    id
    value
    startDate
    endDate
  }
  physicalQuantity {
    name
    baseUnit {
      name
      shorthand
    }
  }
  emissionSources {
    id
    name
  }
}
